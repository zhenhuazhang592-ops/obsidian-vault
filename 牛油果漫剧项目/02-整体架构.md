# 漠玫牛油果漫剧项目 - 整体架构

## 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         前端 (Electron/Web)                       │
│                    React + TypeScript + Tailwind                 │
├─────────────────────────────────────────────────────────────────┤
│                    WebSocket / REST API                         │
├─────────────────────────────────────────────────────────────────┤
│                         后端服务 (Express)                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │
│  │  路由层     │  │  Agent层    │  │  工具层     │            │
│  │  routes/   │  │  agents/   │  │  utils/    │            │
│  └─────────────┘  └─────────────┘  └─────────────┘            │
├─────────────────────────────────────────────────────────────────┤
│                    AI 服务层 (Vercel AI SDK)                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │
│  │  剧本生成   │  │  图像生成   │  │  视频生成   │            │
│  │  AIScript   │  │  AIImage   │  │  AIVideo   │            │
│  └─────────────┘  └─────────────┘  └─────────────┘            │
├─────────────────────────────────────────────────────────────────┤
│                    数据存储层 (SQLite)                            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │
│  │  项目/剧本  │  │  角色/资产  │  │  分镜/视频  │            │
│  │  t_project │  │  t_character│  │  t_shot    │            │
│  │  t_script  │  │  t_assets  │  │  t_video   │            │
│  └─────────────┘  └─────────────┘  └─────────────┘            │
└─────────────────────────────────────────────────────────────────┘
```

## 目录结构

```
momo-comic/
├── docker/                    # Docker 配置文件
├── docs/                      # 项目文档
├── scripts/                   # 构建脚本
├── src/
│  ├── agents/                 # AI Agent 核心模块
│  │  ├── script/            # 剧本生成 Agent
│  │  ├── character/         # 角色生成 Agent
│  │  └── storyboard/        # 分镜生成 Agent
│  ├── lib/                   # 公共库
│  │  ├── initDB.ts          # 数据库初始化
│  │  └── fixDB.ts           # 数据库修复
│  ├── middleware/            # 中间件
│  ├── routes/               # 路由模块
│  │  ├── project/           # 项目管理
│  │  ├── script/            # 剧本管理
│  │  ├── character/         # 角色管理
│  │  ├── storyboard/        # 分镜管理
│  │  ├── image/             # 图片生成
│  │  └── video/             # 视频生成
│  ├── types/                 # TypeScript 类型声明
│  ├── utils/                 # 工具函数
│  │  ├── ai/                # AI 服务封装
│  │  │  ├── text/           # 文本生成
│  │  │  ├── image/          # 图像生成
│  │  │  └── video/          # 视频生成
│  │  ├── db.ts              # 数据库操作
│  │  └── oss.ts             # 文件存储
│  ├── app.ts                 # 应用入口
│  └── router.ts              # 路由注册
├── env/                      # 环境变量配置
├── package.json              # 项目配置
└── tsconfig.json             # TypeScript 配置
```

## 核心数据流

```
1. 用户创建项目 → t_project 表
2. 选择选题 → t_topic 表
3. 调用 Script Agent → 生成剧本 → t_script 表
4. 调用 Character Agent → 生成角色图 → t_character 表
5. 调用 Storyboard Agent → 生成分镜 → t_shot 表
6. 调用 Image Generation → 生成图片 → OSS 存储
7. 调用 Video Generation → 生成视频 → t_video 表
8. FFmpeg 合成 → 最终成片
```

## 核心工作流

```
┌─────────────────────────────────────────────────────────────┐
│                    漫剧制作六步法                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  📝 选题确定                                                │
│      ↓ AI解析：场景/分镜/角色                               │
│  🔧 AI二次校准（魔因漫创）                                  │
│      ↓ 场景优化 → 分镜优化 → 角色锚点                       │
│  🎭 角色图生成（6层锚点）                                   │
│      ↓ AI批量生成 → 人工筛选 → 角色图库                     │
│  🌄 场景图生成（多视角）                                     │
│      ↓ AI批量生成 → 视角校验                               │
│  🎬 分镜图生成（专业模板）                                   │
│      ↓ 提示词融合 → 角色参考图                             │
│  🎥 视频生成（Seedance）                                    │
│      ↓ 三层融合提示词 → 口型同步                           │
│  ✂️ FFmpeg合并 → ⭐ 最终成片                                 │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## 路由统计

| 分类 | 数量 | 主要路由 |
|-----|-----|---------|
| 视频 | 8 | generateVideo, getVideo, saveVideo |
| 分镜 | 10 | generateShotImage, saveStoryboard, getShotList |
| 角色 | 6 | generateCharacter, getCharacter, updateCharacter |
| 剧本 | 5 | generateScript, getScript, updateScript |
| 项目 | 4 | createProject, getProjectList, deleteProject |
| 选题 | 4 | getTopicList, createTopic, updateTopic |
| 任务 | 3 | getTaskProgress, retryTask |

---

## 与对标项目对比

| 维度      | Toonflow | 本项目        |
| ------- | -------- | ---------- |
| 输入      | 小说       | 选题+角色设定    |
| 核心Agent | 大纲+分镜    | 剧本+角色+分镜   |
| 场景      | 通用短剧     | 漠玫轻食餐厅     |
| 角色      | 动态生成     | 固定角色库      |
| 视频模型    | 多模型      | Seedance为主 |
